#include"removeElement.h"

int main()
{
    vector<int> test;
    int num;
    removeElement RemoveElement;
    test.push_back(1);
    test.push_back(2);
    test.push_back(3);
    num = RemoveElement.Solution(test,1);
    getchar();
    return 0;
}